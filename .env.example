# Firebase
VITE_FIREBASE_API_KEY=
VITE_FIREBASE_AUTH_DOMAIN=
VITE_FIREBASE_PROJECT_ID=
VITE_FIREBASE_APP_ID=

# Backend API (not the Vite dev server). Run the Navigator backend here or point to a deployed URL.
VITE_API_URL=http://localhost:8000

# Mock mode: no backend required. Conversations and messages are in-memory; assistant replies are random stock phrases.
# Set to true to try the full chat UI without running the backend.
# VITE_MOCK_CHAT=true

# Feature flags (off by default)
# VITE_FEATURE_BULK_UPLOAD=true  # JSON Chat Viewer: show bulk upload to backend
